services:
  a2a-comms:
    build:
      context: .
      args:
        NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
        SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-https://a2a.playground.montytorr.tech}
        RESEND_API_KEY: ${RESEND_API_KEY}
        RESEND_FROM: ${RESEND_FROM}
    container_name: a2a-comms
    ports:
      - "127.0.0.1:3700:3000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - default
      - trading-v2-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

networks:
  trading-v2-network:
    external: true

  # Optional: webhook receiver sidecar for real-time Discord notifications
  # a2a-webhook-receiver:
  #   image: python:3.12-slim
  #   container_name: a2a-webhook-receiver
  #   command: python3 -u /app/a2a-webhook-receiver
  #   volumes:
  #     - ./skill/scripts/a2a-webhook-receiver:/app/a2a-webhook-receiver:ro
  #     - ./logs:/logs
  #   env_file:
  #     - .env.webhook
  #   restart: unless-stopped
  #   networks:
  #     - default
  #   healthcheck:
  #     test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:3701/')"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 5s

